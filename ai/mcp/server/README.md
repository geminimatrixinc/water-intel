# MCP server

Place the canonical Water-Intel MCP server implementation here.

The intended pattern is to expose structured tools backed by `services\api\` logic rather than duplicating data access code.
